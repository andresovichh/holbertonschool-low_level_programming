#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Tdasdasdasd
 * Return: always zero
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		if (ch != 'q' && ch != 'e')
		putchar(ch);
	putchar('\n');
	return (0);
}
