#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Tdasdasdasd
 * Return: always zero
 */
int main(void)
{
	int i;
	int j;

	for (i = 48; i <= 57; i++)
	{
	for (j = 48; i > j; j++)
		{
		putchar(i);
		putchar(j);
		putchar(44);
		putchar(32);
		}
	}

		putchar('\n');

	return (0);
}
