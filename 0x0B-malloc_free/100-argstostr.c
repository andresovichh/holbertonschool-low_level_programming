#include "main.h"
#include <stdio.h>
#include <stdlib.h>


/**
 * argstostr - returns pointer to 2 dim array of int
 *
 * @ac: width of array
 * @av: height of array
 * Return: Always 0
 */


char *argstostr(int ac, char **av)
{
	if (ac == 0)
		return (0);
	if (*av == 0)
	return (0);
return (0);
}
