#include "main.h"
#include <stdio.h>
#include <stdlib.h>


/**
 * strtow - returns pointer to 2 dim array of int
 *
 * @str: width of array
 * Return: Always 0
 */


char **strtow(char *str)
{
	if (str != 0)
		return (0);
return (0);
}
