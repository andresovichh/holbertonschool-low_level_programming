#include "main.h"

/**
 * _strlen - dasdas
 *
 *
 * @s: dafddsfsd
 * Return: Always total
 *
 */

int _strlen(char *s)
{
	int total = 0;

	for (total = 0; total <= *s; total++)
	{
		total = total + 1;
	}
	return (total);




}
